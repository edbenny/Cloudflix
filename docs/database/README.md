# Database migrations

`003_create_watchlist_table.sql` was recovered from the original project. The other migrations were reconstructed from the SQL queries and response fields used by `backend/app.py`; review them before deploying.
